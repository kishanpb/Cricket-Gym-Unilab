# @package _global_
training:
  task_name: G1CricketBatting
  sim_backend: mujoco
  device: cpu
  no_play: true
algo:
  num_envs: 4
  max_iterations: 10
  num_steps_per_env: 24
  policy:
    actor_hidden_dims: [64, 64]
    critic_hidden_dims: [64, 64]
  algorithm:
    num_mini_batches: 2
env:
  handedness: right
  seed: 1
  scene:
    model_file: src/unilab/assets/robots/g1/g1.xml
    default_keyframe_name: cricket_stand
    entities:
      robot:
        root_body_name: pelvis
        joint_names: &joints
          [left_hip_pitch_joint, left_hip_roll_joint, left_hip_yaw_joint,
           left_knee_joint, left_ankle_pitch_joint, left_ankle_roll_joint,
           right_hip_pitch_joint, right_hip_roll_joint, right_hip_yaw_joint,
           right_knee_joint, right_ankle_pitch_joint, right_ankle_roll_joint,
           waist_yaw_joint, waist_roll_joint, waist_pitch_joint,
           left_shoulder_pitch_joint, left_shoulder_roll_joint, left_shoulder_yaw_joint,
           left_elbow_joint, left_wrist_roll_joint, left_wrist_pitch_joint, left_wrist_yaw_joint,
           right_shoulder_pitch_joint, right_shoulder_roll_joint, right_shoulder_yaw_joint,
           right_elbow_joint, right_wrist_roll_joint, right_wrist_pitch_joint, right_wrist_yaw_joint]
        actuator_names: *joints
        body_names: [pelvis, cricket_bat]
      ball:
        root_body_name: cricket_ball
        body_names: [cricket_ball]
  sim_dt: 0.002
  ctrl_dt: 0.01
  max_episode_seconds: 2.0
  post_step_forward_sensor: false
  auto_reset: true
  observations:
    policy:
      terms:
        joint_pos:
          func: unilab.envs.mdp.joint_pos_rel
        joint_vel:
          func: unilab.envs.mdp.joint_vel_rel
        gravity:
          func: unilab.envs.mdp.projected_gravity_from_sensor
          params: {sensor_name: torso_upvector}
        gyro:
          func: unilab.envs.mdp.builtin_sensor
          params: {sensor_name: torso_gyro}
        ball:
          func: unilab.tasks.manipulation.g1_cricket.task.BallObservation
        contact_snapshots:
          func: unilab.tasks.manipulation.g1_cricket.task.ContactObservation
        last_action:
          func: unilab.envs.mdp.last_action
  actions:
    joint_pos:
      _target_: unilab.envs.mdp.JointPositionActionCfg
      entity_name: robot
      actuator_names: [".*"]
      scale: 0.25
      use_default_offset: true
      clip: {".*": [-1.0, 1.0]}
  events:
    reset_scene:
      func: unilab.envs.mdp.reset_scene_to_default
      mode: reset
    reset_ball:
      func: unilab.tasks.manipulation.g1_cricket.task.ResetBall
      mode: reset
      params: {speed: 4.0}
  terminations:
    time_out:
      func: unilab.envs.mdp.time_out
      time_out: true
    fallen:
      func: unilab.tasks.manipulation.g1_cricket.task.fallen
  policy_observation_group: policy
  critic_observation_group: null

# Construction/curriculum diagnostics only, not a validated cricket objective.
reward:
  upright:
    func: unilab.tasks.manipulation.g1_cricket.task.upright
    weight: 1.0
  action_rate:
    func: unilab.envs.mdp.action_rate_l2
    weight: -0.001
